# --- Local development ---
ML_SERVICE_URL="http://localhost:8000"

# --- Production (Vercel) ---
# Set NEXT_PUBLIC_ML_SERVICE_URL to your HuggingFace Space URL.
# The browser will call the ML service directly (avoids Vercel's 10s timeout).
# NEXT_PUBLIC_ML_SERVICE_URL="https://your-username-pixeltrace-ml.hf.space"
